# 通用模板。按环境请改用:
#   cp env.monitor.test.example monitor.env.test
#   cp env.monitor.prod.example monitor.env.prod
# 仍支持回退文件 monitor.env
#
# IPWO 住宅代理：请到管理中心 →「采集代理」配置（写入引擎库）
# 以下键已废弃，生产请留空；子进程优先用管理员库
# IPWO_PROXY_HOST=
# IPWO_PROXY_PORT=
# IPWO_PROXY_USER=
# IPWO_PROXY_PASS=
# IPWO_PROXY_ZONE=

# 方式二：完整 URL（会覆盖方式一；生产勿用）
# SOFA_HTTP_PROXY=http://user:pass@us.ipwo.net:7878
# SOFA_HTTPS_PROXY=http://user:pass@us.ipwo.net:7878

SOFA_HTTP_PROXY=
SOFA_HTTPS_PROXY=

# Sofascore API（默认走官网同源，避免 api.sofascore.com 403）
# SOFA_API_BASE=https://www.sofascore.com/api/v1
SOFA_REQUEST_TIMEOUT_SEC=90
SOFA_REQUEST_RETRIES=3
SOFA_REQUEST_RETRY_BACKOFF_SEC=3
SOFA_MIN_INTERVAL=0.35

# Live score polling interval (seconds). t3.small 建议 >=300
LIVE_POLL_INTERVAL_SEC=300

# Docker server 容器名（compose project: yuce）；本地 npm run dev 无容器，可设 none 跳过 docker exec
# YUCE_SERVER_CONTAINER=none
YUCE_SERVER_CONTAINER=yuce-server-1

# Monitor HTTP API
SOFA_MONITOR_HOST=0.0.0.0
SOFA_MONITOR_PORT=9004
SOFA_MONITOR_TOKEN=sofascore-monitor-2026

# MySQL（默认不写库；需入库时设 SOFA_WRITE_MYSQL=1）
SOFA_WRITE_MYSQL=0

# Redis（collect_live 写入 tennis:bundle:inplay；须与 server/.env 的 REDIS_URL 一致）
REDIS_URL=redis://46.250.163.215:9015
# TENNIS_CACHE_TTL_SEC=86400

# 进行中简单采集（collect_live.py --filter=false）默认场数
# SOFA_LIVE_SIMPLE_LIMIT=20
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_USER=root
# DB_PASSWORD=
# DB_NAME=tennisv2
