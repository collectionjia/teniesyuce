# Docker 测试环境（docker compose --env-file deploy/test.env ...）
# 不部署 compose 内 redis（无 COMPOSE_PROFILES=with-redis），用 SERVER_ENV_FILE 里的 REDIS_URL
COMPOSE_PROJECT_NAME=yuce-test
APP_ENV=test
SERVER_ENV_FILE=./server/.env.test

# 宿主机端口（可按机器改）
WEB_PORT=9018
BOARD_PORT=8890
